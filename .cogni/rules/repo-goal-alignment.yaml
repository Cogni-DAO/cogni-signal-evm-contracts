id: cogni-git-review-repo-goal-alignment
schema_version: "0.3"
blocking: true
workflow_id: goal-evaluations

# Edit this! Update it with a clear description of the repo's goal.
evaluations:
  - repo-goal: This PR is directly related to building stable smart contracts to DAO management signals to the cogni-git-admin github app.
  # - clear-sub-goals: This PR only uses open source tools and does not introduce new paid dependencies.


# Code-aware capabilities (vendor-prefixed for non-breaking compatibility)
x_capabilities:
  - diff_summary      # Enable enhanced diff summary with file stats
  - file_patches      # Include actual patch content for analysis

success_criteria:
  neutral_on_missing_metrics: true
  require:
    - metric: repo-goal
      gte: 0.7        # Lower required score, This will be a hard evaluation for the AI to do well
    # - metric: clear-sub-goal
    #   gte: 0.8